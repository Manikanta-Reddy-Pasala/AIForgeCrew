.venv
**/__pycache__
**/*.pyc
web/node_modules
web/dist
.git
.pytest_cache
graphify-out
*.db
.aiforge
.aiforge-worktrees
data
docs
evals
# Runtime/local data that must never bake into the image (present on a used
# checkout, absent on a clean clone) — these ballooned the COPY layer.
models
neo4j-data
neo4j-logs
hf-cache
*.pptx
*.log
# Secrets and build output must never enter the image — the recursive
# COPY in the Dockerfile is bounded by THIS file.
.env
.env.*
*.pem
*.key
**/.aiforge
security
sonar
test-results
.scannerwork
coverage.xml
# Build output and caches: the image copies the whole checkout (the sandbox
# syncs it into its state volume), so none of this may ride along.
dist
build
*.egg-info
.ruff_cache
blackduck
.uv-cache
.npm-cache
web/.vite
**/node_modules

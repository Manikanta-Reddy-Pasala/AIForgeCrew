<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.aiforge.sync-memory</string>

    <key>ProgramArguments</key>
    <array>
        <string>/bin/bash</string>
        <string>-lc</string>
        <string>DIR=push /bin/bash {{SYNC_SCRIPT}} &gt;&gt; {{LOG_FILE}} 2&gt;&amp;1</string>
    </array>

    <!-- Run daily at 02:00 local time. If Mac was asleep, runs at wake. -->
    <key>StartCalendarInterval</key>
    <dict>
        <key>Hour</key>
        <integer>2</integer>
        <key>Minute</key>
        <integer>0</integer>
    </dict>

    <!-- Also run once 60s after load so first install smokes-tests itself. -->
    <key>RunAtLoad</key>
    <false/>

    <key>StandardOutPath</key>
    <string>{{LOG_FILE}}</string>
    <key>StandardErrorPath</key>
    <string>{{LOG_FILE}}</string>

    <!-- Avoid thundering herd if Mac wakes from long sleep -->
    <key>ThrottleInterval</key>
    <integer>3600</integer>

    <key>EnvironmentVariables</key>
    <dict>
        <key>PATH</key>
        <string>/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin</string>
        <key>SSH_HOST</key>
        <string>manikanta@192.168.70.185</string>
    </dict>
</dict>
</plist>
